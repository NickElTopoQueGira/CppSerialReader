#ifndef MYFILE_H
#define MYFILE_H

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include <ctime>
#include <sstream>

// Functions
int createFile(char* path);
int creteFileName (char* filename);
int isFileExist(char* path);

#endif